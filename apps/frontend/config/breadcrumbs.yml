ice_breadcrumbs:
  _root: { name: 'Home', route: homepage }
  _separator: ' → '
  _lost: ''
  _default_case: 'ucwords'

  _video:
    index: [{ name: 'Video', route: video }]

  general:
    index: [{ name: 'Home', route: homepage }]

  aent:
    americanPickers: [{ name: 'As Seen on American Pickers' }]
    pawnStars: [{ name: 'Pawn Stars' }]
    storageWars: [{ name: 'Storage Wars' }]
    collectible:
      - { name: '%Collection%', route: collection_by_slug, model: true, subobject: Collection }
      - { name: '%Name%', model: true }

  collection:
    index:
      - { name: '%Name%', model: true }
    collectible:
      - { name: '%Collection%', model: true, route: collection_by_slug, subobject: Collection }
      - { name: '%Name%', model: true }

  collections:
    index: [{ name: 'Collections' }]

  collector:
    index:
      - { name: 'Collector %DisplayName%', model: true }
    signup:
      - { name: 'Signup' }

  marketplace:
    index: [{ name: 'Marketplace' }]
    categories:
      - { name: 'Marketplace', route: marketplace }
      - { name: 'Categories' }
    browse:
      - { name: 'Marketplace', route: marketplace }
      - { name: '%Name%', model: true }

  search:
    index: [{ name: 'Search Results', route: search }]
    collections:
      - { name: 'Search', route: search }
      - { name: 'Collections' }
    collectors:
      - { name: 'Search', route: search }
      - { name: 'Collectors' }
    collectibles:
      - { name: 'Search', route: search }
      - { name: 'Collectibles' }
    collectiblesForSale:
      - { name: 'Search', route: search }
      - { name: 'Collectibles For Sale' }
    blog:
      - { name: 'Search', route: search }
      - { name: 'Blog Articles' }
    video:
      - { name: 'Search', route: search }
      - { name: 'Video' }

  categories:
    category:
      - { name: '%Name%', model: true, title: 'Category: %Name%' }
