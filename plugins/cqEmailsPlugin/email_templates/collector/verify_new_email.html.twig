{% extends "common/base.html.twig" %}

{% block content %}
  <p style="margin-bottom: 10px; font-weight: bold;">
    Dear {{ collector.displayName }},
  </p>
  <p>
    Your email has been changed to <strong>{{ collector_email.email }}</strong> and needs verification
    {{ link_to('here', '@collector_verify_email', { hash: collector_email.hash }, { absolute: true}) }}.
  </p>
{% endblock %}
